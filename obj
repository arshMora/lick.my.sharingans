let numDecomp = {
  hundreds: 0,
  tens: 0,
  units: 0,
  decomp: function(num){
    if (num.length !== 3)
      return console.error("An incorrectly entered number")

    this.hundreds = num[0]
    this.tens = num[1]
    this.units = num[2]
  }
}

numDecomp.decomp("412");
console.log(numDecomp)
